package com.dbx.archfixture.connection;

/** An H2-backed store inside connection: what obligation 2 says the module never holds. */
public final class CredentialRepository {

    public void save(String ciphertext) {
    }
}
