package com.dbx.archfixture.condition.api;

import com.dbx.archfixture.preflight.api.PreflightApi;

/** Half of rule 2's cycle. Both halves go through api, so only the cycle rule bites. */
public final class ConditionApi {

    private final PreflightApi preflight = null;
}
