# The fourteen module packages of ADR-0036 are still shells, so a rule whose subject set is empty
# would abort the run instead of passing. The fixtures under com.dbx.archfixture are what keeps an
# empty rule from being mistaken for an enforced one (ADR-0022).
archRule.failOnEmptyShould=false
