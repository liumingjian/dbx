# The L1 duration line is measured from the start of the configuration phase. A reused
# configuration cache skips that phase, which would make the reported duration meaningless, so the
# configuration cache stays off until the rung learns to measure itself another way.
org.gradle.configuration-cache=false
org.gradle.caching=true
org.gradle.parallel=true
