/**
 * 阶段四 执行确认 (#37): the summary, the 写冻结 confirmation, and the start.
 *
 * `StageConfirm` is what `MigrationWizardStagePage`'s `stageContent` map renders; the two
 * panels beside it are internal to the stage.
 */
export { StageConfirm } from './StageConfirm';
